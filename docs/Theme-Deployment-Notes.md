# Theme Deployment Notes — curtin-pc-shop

Read this before making any change to the `curtin-pc-shop` theme or deploying a new zip. These are hard-won lessons from real production incidents, not style preferences.

## 1. Never add a `woocommerce.php` file to the theme root

This is an architectural constraint, not a choice. WooCommerce's template loader (`WC_Template_Loader::get_template_loader_files()`) unconditionally appends `woocommerce.php` to its template search list, and does so *after* its own filter hook runs — so no filter can remove it once the file exists. If present, it silently wins over `woocommerce/archive-product.php` and `woocommerce/single-product.php`, rendering the shop archive and single product pages as a bare title + description + bullet list (no images, no price, no Add to Cart button), with no error thrown. Page caching can hide this for weeks.

Cart, Checkout, and My Account don't need a dedicated template — they are plain WordPress Pages that WooCommerce's template loader never touches, so `page.php` renders them correctly on its own.

This exact mistake has been made three times on this project — most recently on 2026-07-11, when it was reintroduced locally (bumped to v2.4.4) without reaching production. If you're about to add a `woocommerce.php` file back, stop and re-read this section.

**Before trusting the local project folder's theme files, confirm their version number actually matches what Theme Details shows on the live site.** Don't assume the local copy in this folder is current just because it's the working source — another session may have changed it without deploying, or deployed and been reverted. Check Appearance → Themes → Theme Details on the live site for the true `CPC_VERSION` before building on top of local files.

## 2. When rebuilding the theme zip mid-session, always use a brand-new output folder

The build sandbox's file view can go stale on a path that was written to earlier in the same session — content can appear old or truncated when zipping, even though the actual file is correct. Increment the build folder name every rebuild (`build-241` → `build-242` → `build-243`...) rather than overwriting one used earlier.

## 3. Deploy via Appearance → Add Theme → Upload, try "Replace installed with uploaded" first

Only fall back to delete-then-fresh-install if that fails with a permissions error. The delete/reinstall path briefly leaves the site with no active theme, which is real outage risk — treat it as a last resort, not the default.

## 4. Verify every deploy in an authenticated, cache-busted browser session

Not a generic web fetch — those returned stale/cached content during past deploys. Check at minimum: the shop archive, a single product page, and cart/checkout. A broken template can look completely fine from wp-admin while the public pages are broken.

## 5. If the site goes down, get real PHP error logs before guessing

The site is self-hosted on Unraid via Docker (SWAG reverse proxy) — not VentraIP or other shared hosting. Pull logs via `docker logs` on the WordPress container rather than relying on WordPress's generic "critical error" message, which hides the actual cause.

## 6. Bump versions on every change

`CPC_VERSION` in `functions.php` and `Version:` in `style.css` should match and increment on every deploy, so cache-busting and "what's actually live" checks stay meaningful. **Live is v2.6.7** (2026-07-12, verified in an authenticated, cache-busted browser after deploy — home, olive-oil, /art-cards/, single product and cart all checked; "Replace installed with uploaded" confirmed 2.6.6 → 2.6.7). **v2.6.8–v2.6.12 were each built and handed to Adam to upload/verify himself** (not deployed by the assistant); the latest zip (v2.6.12) supersedes them all, folding in everything below — confirm live Theme Details shows 2.6.10 before trusting the source folder as current. NOTE: the live olive-oil product page fetched 2026-07-12 still showed old "Art Cards"/`/cards/` nav labels, i.e. **live was running a build older than 2.6.7** at that moment — so the deployed version is genuinely uncertain; verify Theme Details after uploading.

**v2.6.15** (2026-07-12) — made the front-page art-cards hero carousel **swipeable**. Added touch-swipe + mouse-drag navigation to the carousel JS (in the `[data-cpc-carousel]` init): `touchstart/touchmove/touchend` and `mousedown`/`mouseup`, 40px horizontal threshold, left→next / right→prev, wraps around; vertical scroll still passes through (only horizontal-dominant moves `preventDefault`, touchmove listener is `passive:false` for that; touchstart is `passive:true`); auto-rotate pauses during the drag and resumes after; a capture-phase `click` handler swallows the click that follows a swipe so a swipe on a card doesn't open its product link. Dots + auto-rotate + reduced-motion behaviour from 2.6.13 unchanged. CSS got a small affordance rule: `.cpc-carousel{touch-action:pan-y;cursor:grab;user-select:none}` + `:active{cursor:grabbing}`. **JS change → assets renamed `curtin-266.*` → `curtin-267.*`** (§8 nginx cache-bust): `curtin-267.js` = clean `curtin-265.js` base + the new swipeable carousel IIFE (verified `curtin-265.js` is a byte-exact prefix of `curtin-266.js` first, so rebuilding on 265 drops nothing); `curtin-267.css` = `curtin-266.css` + the swipe affordance rule; both enqueues + note comment in `functions.php` updated, `CPC_VERSION`/`style.css` → 2.6.15. `front-page.php` unchanged from 2.6.14. Built from the clean `curtin-pc-shop-v2.6.14.zip` base in `/tmp` (`cp`/heredoc/`sed`, per §9); NUL scan clean, all PHP + the JS/CSS braces/parens balanced, no root `woocommerce.php`, zip re-extracted and re-scanned. Zip written to project via Python truncating write; source `theme/` synced (functions.php, style.css + new curtin-267.css/js). Not yet verified live. **On the next CSS/JS change bump to `curtin-268.*`.**

**v2.6.14** (2026-07-12) — PHP-only tweak on top of 2.6.13: removed the duplicate price + shop button from the left copy of **both** front-page heroes (`.cpc-price-row` and `.cpc-cta-row` blocks deleted from `front-page.php`), since the product card in each hero's art slot already shows the price and an Add-to-cart/View button. Each hero copy now ends at its `.cpc-hero-lede`. `$hero_price`/`$hero_url`/`$hero_alt` are now unused but left in place (harmless). **No CSS/JS change → assets stay `curtin-266.*`** (no nginx cache-bust rename); `CPC_VERSION`/`style.css` → 2.6.14. Built from the clean `curtin-pc-shop-v2.6.13.zip` base in `/tmp` (regex block-removal + `sed`, per §9); NUL scan clean, all PHP braces/parens balanced, no root `woocommerce.php`, zip re-extracted and re-scanned. Zip written to project via Python truncating write; source `theme/` folder synced (front-page.php, functions.php, style.css). Not yet verified live. **On the next CSS/JS change bump to `curtin-267.*`.**

**v2.6.13** (2026-07-12) — front-page hero redesign (PHP + CSS + JS). `front-page.php` reworked: (1) the **olive-oil hero** (top) no longer shows the big olive image/SVG in `.cpc-hero-art` — it now renders the Curtin Gold product as a **product card** in the same format as the old bottom-collection cards (shared new helper `cpc_fp_render_card($p)` = the exact `.cpc-card` markup the grids use); the left hero copy (eyebrow, H1, lede, price, "Shop Curtin Gold" CTA) is unchanged, so price/CTA now appear both in the copy and on the card (intentional per Adam's request). (2) the **Butterfly Garden hero** (second) is now an **auto-rotating carousel of 3 art-card product cards** — same `wc_get_products` query that fed the old collection (publish, limit 3, `product_cat NOT IN olive-oil`, menu_order). Copy changes: eyebrow "The Butterfly Garden" → **"Our Art Cards"**; lede "One big community artwork…" → **"Three big community artworks…"** (pluralised "artwork" so it reads correctly); the single-image `.cpc-credit` chip was dropped (a fixed credit made no sense over rotating cards); left price stays a static "$10.00". (3) the redundant bottom **"The collection" (`#cpc-cards`) section was removed** entirely (its 3 cards now live in the rotating hero). Story band + trust strip unchanged. Carousel = grid-stacked slides (`.cpc-carousel-track{display:grid}`, all `.cpc-slide` on `grid-area:1/1` so the track auto-sizes to the tallest card, no layout jump), opacity/visibility fade, dot controls, `data-interval="4500"`; JS is a new self-contained IIFE appended to the assets file (pauses on hover + when the tab is hidden, and **honours `prefers-reduced-motion` by not auto-rotating**). **This is a CSS+JS change → assets renamed `curtin-265.*` → `curtin-266.*`** (§8 nginx cache-bust): `curtin-266.css` = `curtin-265.css` + the new `.cpc-hero-card`/`.cpc-carousel`/`.cpc-slide`/`.cpc-dot` rules; `curtin-266.js` = `curtin-265.js` + the carousel IIFE; both enqueue lines + the note comment in `functions.php` updated, `CPC_VERSION`/`style.css` → 2.6.13. Built from the clean `curtin-pc-shop-v2.6.12.zip` base entirely in `/tmp` via heredoc/`cp`/`sed` (no file-tool-edit-then-shell-read, per §9); NUL scan clean, braces/parens balanced across all PHP (real `php -l` unavailable — no php-cli, couldn't apt-install), no root `woocommerce.php`, packaged zip re-extracted and re-scanned clean. Zip written into the project folder via Python truncating write; source `theme/` folder synced to 2.6.13 (front-page.php, functions.php, style.css + new curtin-266.css/js). Not yet verified live — Adam to upload and check in an authenticated, cache-busted browser (§4). **On the next CSS/JS change bump to `curtin-267.*`.**

**v2.6.12** (2026-07-12) — CSS fix on top of 2.6.11: the category description on the `/all-products/` (and `/product-category/`) archives was rendering at only ~a third of the row width. Cause: the grouped-view description div carried `.cpc-hero-lede`, whose desktop rule caps `max-width:420px`. Fix: dropped `cpc-hero-lede` from that div in `archive-product.php` (the empty-state "products will appear here soon" line still legitimately uses `.cpc-hero-lede`), and added a full-width `.cpc-cat-desc` rule (no max-width, so it spans the full container row, with `p` paragraph spacing) — applies to both the solo product-category `<h1>` intro and the grouped shop `<h2>` sections. **This is a CSS change → assets renamed `curtin-264.*` → `curtin-265.*`** (§8 nginx cache-bust): `curtin-265.css` = `curtin-264.css` + the new `.cpc-cat-desc` rules; `curtin-265.js` = unchanged copy of `curtin-264.js` (renamed to keep the pair consistent, per §6); both enqueue lines + the note comment in `functions.php` updated. Built from the clean `curtin-pc-shop-v2.6.11.zip` base via shell `cp`/`sed` (no file-tool-edit-then-shell-read, per §9); NUL scan clean, braces/parens balanced, no root `woocommerce.php`, packaged zip re-extracted and re-scanned. Same mount quirk as 2.6.11 (zip built in `/tmp`, written into the project folder via Python truncating write). Zip: `curtin-pc-shop-v2.6.12.zip`. Source `theme/` folder synced to 2.6.12 (archive-product.php, functions.php, style.css + new curtin-265.css/js). **On the next CSS/JS change bump to `curtin-266.*`.**

**v2.6.11** (2026-07-12) — `woocommerce/archive-product.php` rewritten to be fully category-driven, PHP-only (no CSS/JS change, so **assets stay `curtin-264.*`** — no nginx cache-bust rename). Fixes four issues Adam reported on the `/all-products/` (shop base = hidden "All products" page 114) archive: (1) the per-card meta was a hard-coded string (`Set of four · blank inside` / `250mL · extra virgin olive oil`) — now every card's meta is the product's own `get_short_description()`; (2) the olive oil listed under the single hard-coded "Art cards" `<h2>` because the template ran one flat `have_posts()` loop over ALL products — now products are **grouped by their real product category** via `get_terms('product_cat', hide_empty, top-level only, excluding default_product_cat)` + a per-category `wc_get_products(category=slug)` grid, so olive oil sits under Olive oil and cards under Art cards; (3) removed the `.cpc-cardstory` "Read the story behind the cards" `<details>` band at the foot of the page; (4) removed the hard-coded top hero intro ("Curtin Primary P&C Art Card Series" H1 + lede that only described cards) — instead **each category renders its WooCommerce category Description (`term_description`) directly below its title** (Adam populated the Description field on both categories in WooCommerce → Products → Categories on 2026-07-12; note the Store API `/wc/store/v1/products/categories` endpoint CACHES and still reported empty descriptions right after he saved — `term_description()` reads live from the DB so the template is unaffected). The same template also drives the single `/product-category/<slug>/` archives: those now show that one category's name (`<h1>`), its description, then its products (helper `cpc_render_category_block($term, $solo)` switches `<h1>`/`.cpc-collection-intro` for solo vs `<h2>`/`.cpc-collection-head` + `.cpc-hero-lede` for the grouped shop view; card markup factored into `cpc_render_product_card()`). Reuses only existing `.cpc-*` classes (plus an inert `.cpc-cat-desc` hook, no CSS rule). Built from the clean `curtin-pc-shop-v2.6.10.zip` base with the new file written by quoted heredoc in the shell (never file-tool-edited-then-shell-read, per §9); NUL scan clean, brace/paren balanced, no root `woocommerce.php`, re-extracted the packaged zip and re-scanned. NOTE: the mount refused `zip`'s in-place replace AND `rm` of the leftover 0-byte file ("Operation not permitted") — built the zip in `/tmp` then wrote it into the project folder via a Python `open(dst,'wb')` truncating write, which succeeded. Zip: `curtin-pc-shop-v2.6.11.zip`. Source `theme/` folder synced to 2.6.11 (3 files: woocommerce/archive-product.php, functions.php, style.css).

**v2.6.10** (2026-07-12) — product-gallery fix, PHP-only. `woocommerce/single-product.php`: the gallery thumbnails were built from the product's gallery images ONLY, and the featured (primary) image was shown in the main slot but had NO thumbnail. So once a visitor clicked a gallery thumb, the main image swapped and there was no way back to the primary image (reported on the Curtin Gold olive oil product, which has one gallery image = the angle shot). Fix: when gallery images exist AND the product has a featured image, the featured image is now prepended as the FIRST thumbnail (`full` = the main `large` src, `thumb` = `woocommerce_gallery_thumbnail`); the render loop already marks index 0 `cpc-thumb-active`, so the primary image is the active/first thumb and clicking it restores the main image. No CSS/JS change — the existing `curtin.js` thumb-swap handler drives it — so **assets stay `curtin-264.*`** (no nginx cache-bust rename needed). Built from the clean `curtin-pc-shop-v2.6.9.zip` base via string edits in the shell (never re-read a file-tool-edited file before zipping, per §9); balanced-brace structural check passed (real `php -l` was unavailable in the sandbox this session — no php-cli, no root to install). NUL scan clean, no root `woocommerce.php`, verified after re-extracting the final zip fr